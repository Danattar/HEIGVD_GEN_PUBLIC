<UserControl x:Class="GTTClientFrontend.Views.TaskBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GTTClientFrontend.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <TextBox Name="TaskName" DockPanel.Dock="Top"/>
        <DockPanel DockPanel.Dock="Bottom">
            <Button Name="Submit" Content="Submit"/>
            <Button Name="Reset" Content="Reset"/>
        </DockPanel>
        <DockPanel DockPanel.Dock="Right">
            <Button Name="TimerStartStop" Content="{Binding TimerLabel}" DockPanel.Dock="Top"/>
            <Label Name="EllapsedTime"/>
        </DockPanel>
            <TextBox Name="TaskDescription" />
    </DockPanel>
</UserControl>

<views:AdvancedUserControl x:Class="GTTClientFrontend.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:GTTClientFrontend.Views"
             mc:Ignorable="d" 
             Background ="#0180aa" 
             d:DesignHeight="450" d:DesignWidth="800"
             Padding="10,10,10,0"
             Height="200" Width ="600">
    <DockPanel >
       <DockPanel DockPanel.Dock="Bottom" LastChildFill="False" HorizontalAlignment="Right">
            <Button Name="Cancel" Content="Cancel"/>
            <Button Name="Save" Content="Save" />
        </DockPanel>
        <DockPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Enter username"  DockPanel.Dock="Left"/>
            <TextBox Name="Username" DockPanel.Dock="Left" Width="400"
                     Loaded="TextBox_Loaded" />
        </DockPanel>
     </DockPanel>
</views:AdvancedUserControl>
